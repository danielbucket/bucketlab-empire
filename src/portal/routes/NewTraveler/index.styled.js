import { styled } from 'styled-components';
import { PageLayout, StyledImage } from '../../style/root.style.js';

export const NewTravelerStyle = styled(PageLayout)``;
export const ImageStyle = styled(StyledImage)``;