import style from 'styled-components';

import { PageLayout, StyledImage, StyledContent } from 'src/root/style/root.style.js';

export const MythtaTLayout = style(PageLayout)``;
export const ImageStyle = style(StyledImage)``;
export const ContentStyle = style(StyledContent)``;